import React from "react";
import Pages from "./pages";

export default class App extends React.Component {
  render() {
    return (
      <div className="grid">
        <Pages />
      </div>
    );
  }
}
